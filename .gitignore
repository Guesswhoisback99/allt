node_modules/
dist/
.DS_Store
*.log
*.png
.playwright-mcp/

# Local env overrides (real patient paths) — never commit.
.env
.env.local
.env.*.local

# All CSV data lives outside the repo or locally only. Nothing in data/ is tracked.
data/
