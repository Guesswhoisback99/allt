node_modules/
dist/
.DS_Store
*.log

# Local env overrides (real patient paths) — never commit.
.env
.env.local
.env.*.local

# Real patient CSVs MUST live outside this repo (e.g. ~/labs/<patient>/).
# Only sample files are tracked.
data/*
!data/*.sample.csv
